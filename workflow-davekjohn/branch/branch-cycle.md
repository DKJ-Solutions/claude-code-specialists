# `feat/measure-skill` cycle · 20260822-102144

## PLAN

- [x] Establish what the platform already provides before designing anything: `claude plugin details`
      (authoritative token cost) and `claude plugin eval` (behavioural scoring with ablation) both
      exist and are unused here; `/skill-doctor` does NOT exist in this build, so nothing is designed
      on it.
- [x] Measure the nulmeting that justifies the work: 18 skills, ~3,650 always-on tokens, never
      re-measured since ~1,245 at `v2.10.0`.
- [x] Put the two course-defining questions to Dave: hosting (chose `workflow-davekjohn`) and scope
      (chose pass 1 + pass 2 now, pass 3 designed but not built).

## CREATE

- [x] `scripts/maintenance/measure-skill.ps1` -- pass 1 (cost, with a merged baseline and its delta)
      and pass 2 (wall-clock, read-only invocations only).
- [x] `scripts/lib/measure-skill-lib.ps1` -- the parsing and formatting half, split out so a suite can
      pin the parser without a `claude` on the machine.
- [x] `MeasureArgs` + `MeasureDeclared` in the shared-scripts registry: the read-only invocation is
      declared beside each registration, and absent means never executed.
- [x] `plugins/workflows/workflow-davekjohn/skills/measure-skill/SKILL.md` -- every parameter
      documented (check 18), the consumer invocation form, and pass 3's four required flags recorded.
- [x] Registry entries for the script and the lib; mirrors generated by `build-shared-scripts.ps1`.
- [x] `README.md`: both `<!-- skills:all -->` spans, which the lint gate refused until they named it.
- [x] `scripts/maintenance/baselines/skill-cost.json` -- the first baseline, 18 skills, committable
      because the figures come from an API rather than from this machine.

## TEST

- [x] `scripts/tests/measure-skill.tests.ps1` -- 57 asserts: the two token notations, the formatters
      asserted UNDER a Dutch culture (an en-US-only test would have passed while the bug shipped), the
      parser against captured 19-row output, both cross-checks including that the rounding gap is NOT
      a problem, and the registry safety invariants with `cut-release` pinned as never declarable.
- [x] Lint gate green: `check-plugin-integrity.ps1`, 0 errors.
- [~] Dropped a hand-rolled full-suite sweep: it spawned a fresh `powershell` per suite, ran past 600s
      into the background, and would have competed with the gate's own run for the machine. `open-pr`
      runs all suites through `Invoke-TestSuiteGate` and blocks on any failure, so the coverage is not
      lost -- only the duplicate.

## DEPLOY

- [x] Nothing here: opening, merging and folding are post-merge work and belong under "Where I left
      off" rather than in a step list the gate reads before the push.

## Where I left off

The work is complete and both gates are green locally. What remains is the ordinary chain: `open-pr`
(which re-runs lint + suites), wait for `lint-en-tests`, merge, fold.

Pass 3 is deliberately unbuilt. If it is picked up, it starts with `claude plugin eval init` -- this
repo has zero eval suites -- and the four non-negotiable flags are on the skill page.

One thing worth a second look at review: `measure-skill` has no `disable-model-invocation` flag, unlike
most of this plugin's skills. That is deliberate -- it is a read-only report in its default mode, and
"what does this skill cost?" is a question the model should be able to answer -- but it is the one
judgement call here that is a preference rather than a measurement.
