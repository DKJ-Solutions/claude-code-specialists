## `docs/v4-5-0-release-note` progress

### Steps

- [x] Read the cut's draft, the seven writing tests in the `cut-release` skill, and the v4.4.0 note as the
      register model
- [x] Rewrite *For consumers* from the tier-2 entries: second person, urgency-ordered, action stated or
      "no action needed" said outright, and no link into `development/` or `internal/` (lint check 25)
- [x] Write *What it is worth* — the release's theme named, plus where the reports came from
- [x] Write *What was still open at this release* — #596's structural half, the fold docstring, the draft's
      branch language, the un-timed release, the out-of-order publish
- [x] Recover what timing evidence git and the API actually support, and report it as a population rather
      than as one run
- [x] Lint + all suites green
- [~] The end-to-end release total — impossible here rather than skipped: no clock was noted before the cut,
      and it cannot be reconstructed. Recorded as an open item instead of estimated.

### Where I left off

Done — the chain is at the PR step.
