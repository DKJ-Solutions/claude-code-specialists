## `docs/v3-8-0-release-documents` progress

### Steps

- [x] `releases/highlights/3.x/3.8.0.md` — rewrite the 929-line draft into a consumer document. The
      one thing a consumer must *act* on (enable the workflow pack or lose `ship-pr` at the next
      update) leads, with the commands; everything that arrives on its own follows; the `RELEASE.md`
      removal gets its own section because it takes away a file the docs used to send them to.
- [x] `releases/internal/3.x/3.8.0.md` — generated by `new-internal-note.ps1` and filled in: what the
      organisation gets, in time and reach rather than in file names.
- [x] Fill in the changelog entry
- [~] The stale `CHANGELOG.md` intro found during the release inspection is NOT repaired here. It is
      its own defect with its own cause (write-once text that passes through every cut unchecked), and
      folding it into the release-documents PR would put two unrelated changes in one diff. Recorded
      in the internal note's open-items section and left as the next branch.

### Where I left off

Finished. Both documents land via this PR; the release commit and tag `v3.8.0` are already public, and
the GitHub Release was published with the developer notes as its body.
