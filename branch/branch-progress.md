## `docs/gate-counts-and-runner-lesson` progress

### Steps

- [x] `CLAUDE.md:410` — the dated coverage sentence stops stating a suite count at all
- [x] `05-15-extension.md:202` — `26 siblings` → `30`, the live present-tense figure
- [x] `05-15-extension.md` — record the hand-rolled-runner lesson in that same paragraph, with the rule that separates a dated count from a live one
- [x] Copy edit on the diff (Edith #17) — two factual overstatements found, both verified against the tree and repaired in both places: only 2 of the six suites assert *"lint gate green on the repo"* (not four), and only 3 of them invoke the lint gate at all, so the lesson keys on the shared **tree** instead
- [x] Write the changelog entry: body + both tiers scored (tier 0 → 3, tier 2 → N/A, nothing here ships)

### Where I left off

Ready for the PR. Note for whoever reads this next: both corrected claims came from the locked topic rather
than from the tree, which is why the copy edit caught them — a lock is a record of a measurement, and its
derived statements need the same verification as an inbound report's.
