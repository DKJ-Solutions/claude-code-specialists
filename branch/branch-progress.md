## `docs/the-cadence-is-counted` progress

### Steps

- [x] Recount the cadence from the tags (the previous figure had moved) and classify by bump type
- [x] Establish the fixed gate cost per bump type from the `v4.4.0` measurement, citing each leg
- [x] Verify that a patch carries no document, against `releases/consumer/` and `releases/internal/`
- [x] Measure the delivery side (merge → next tag) and simulate the batching alternatives
- [x] Write the answer into open number 3 of Nolan's lens, with its limits
- [x] Repoint the two older paragraphs the measurement supersedes (the "~17 minutes" estimate and the
      three-numbers intro)
- [x] Write the changelog entry
- [x] Copy edit the diff — link targets resolve, table indentation holds inside the numbered list, and
      the diff is scoped to the three intended files
- [x] Run the lint gate before pushing (0 errors, 0 findings)

### Where I left off

The work is done: the count is recorded in Nolan's lens and the entry is written. The cadence decision
itself is deliberately left open — it is Dave's, and counting it does not make it.
