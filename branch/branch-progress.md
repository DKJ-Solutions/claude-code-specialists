## `docs/source-repo-script-path` progress

### Steps

- [x] Add the source-repo sentence beside the run command on the eight silent skill pages
      (`continue`, `lock`, `new-branch`, `open-pr`, `ship-pr`, `park`, `fold-changelog`, `fix-mojibake`)
- [x] Complete `adopt-config`'s existing `${CLAUDE_PLUGIN_ROOT}` note, which has the hand-typing half
      but not the source-repo half — and annotate its `check-script-contract` command, which this repo
      does run as a gate, while noting that adopting the source's config *into the source* is the one
      command with no source-repo case
- [x] Sharpen `cut-release`'s existing sentence from permissive ("works as before") to corrective
      (the mirror is a released snapshot and lags)
- [x] Record the measurement once, in `scripts/README.md`, where this repo's own developers read it
- [x] Fill in the changelog entry (both tiers, with scores)
- [x] Lint + test gates green

### Where I left off

