## `docs/v4-6-0-release-note` progress

### Steps

- [x] Rewrite the *For consumers* section from the cut's draft against the seven writing tests
- [x] Write *What it is worth* — the release-inspection argument, the shared defect shape, the four-gate-runs cost
- [x] Write *What was still open at this release* — the tracked cache file, the two unguarded hooks, the unproven audience knob, the two-point timing
- [x] Write the step-0a timing section: clock start, eight measured legs, frozen subtotal, and why three legs cannot be measured yet
- [x] Gates, PR, merge, fold

### Where I left off

Document merged. Two steps of the release checklist follow this branch rather than belonging to it, which is
why they are not steps above: publishing the GitHub Release (step 5, which waits for this merge so the
attachments exist), and the second timing pass that adds the end-to-end total — the skill asks for that in
its own small pull request, because the number does not exist until publishing has happened.

